[package]
name = "riftnet-protocol"
version = "0.1.0"
edition = "2021"

[dependencies]
riftnet-core = { path = "../core" }
zerocopy = { version = "0.7", features = ["derive"] }
static_assertions = "1.1"
parking_lot = "0.12.5"